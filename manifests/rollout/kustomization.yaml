apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rollout.yaml
  - service.yaml
  - servicePreview.yaml
  - serviceMonitor.yaml
  - serviceAccount.yaml
  - hpa.yaml
  - pdb.yaml

patches:
  - path: hpaRolloutReference.yaml
  - path: serviceAccountRollout.yaml

configurations:
  - servicemonitorMatchLabels.yaml
  - rolloutMatchLabels.yaml
  - rolloutNameReference.yaml
