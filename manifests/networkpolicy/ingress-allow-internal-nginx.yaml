apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: networkpolicy-ingress-allow-internal-nginx
spec:
  podSelector:
    matchLabels:
      app: filestorageservice
  policyTypes:
  - Ingress
  ingress:
  - from:
      - podSelector:
          matchLabels:
            infra/app: ingress-nginx-internal
            infra/component: controller
      - namespaceSelector:
          matchExpressions:
            - key: kubernetes.io/metadata.name
              operator: In
              values: [ingress-internal]
    ports:
    - port: 3000
      protocol: TCP
